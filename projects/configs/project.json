{
    "name": "configs",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "prefix": "tui",
    "projectType": "library",
    "sourceRoot": "projects/configs",
    "targets": {
        "publish": {
            "executor": "nx:run-commands",
            "options": {
                "command": "npm publish ./dist/{projectName} --access=public --ignore-scripts"
            }
        }
    }
}
