{
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "name": "stylelint-config",
    "prefix": "tui",
    "projectType": "library",
    "sourceRoot": "projects/stylelint-config",
    "targets": {
        "build": {
            "executor": "@nx/rollup:rollup",
            "options": {
                "additionalEntryPoints": ["projects/{projectName}/taiga-specific.ts"],
                "assets": ["projects/{projectName}/README.md", "projects/{projectName}/docs"],
                "compiler": "tsc",
                "external": "all",
                "format": ["esm"],
                "generateExportsField": true,
                "main": "projects/{projectName}/index.ts",
                "outputPath": "{workspaceRoot}/dist/{projectName}",
                "sourceMap": true,
                "tsConfig": "projects/{projectName}/tsconfig.json"
            },
            "outputs": ["{workspaceRoot}/dist/{projectName}"]
        },
        "publish": {
            "dependsOn": [
                {
                    "dependencies": false,
                    "params": "ignore",
                    "target": "build"
                }
            ],
            "executor": "nx:run-commands",
            "options": {
                "command": "npm publish ./dist/{projectName} --access=public --ignore-scripts"
            }
        }
    }
}
